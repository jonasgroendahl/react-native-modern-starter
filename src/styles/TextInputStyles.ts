import {StyleSheet} from 'react-native';

export const TextInputStyles = StyleSheet.create({
  input: {
    marginBottom: 10,
    paddingLeft: 10,
    backgroundColor: '#eee',
    borderColor: '#eee',
  },
});
